import { PreventForwardDirective } from './prevent-forward.directive';

describe('PreventForwardDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventForwardDirective();
    expect(directive).toBeTruthy();
  });
});
